// Constructor function
function Product(image, code, name, quantity, price, subtotal) {
    this.image = image;
    this.code = code;
    this.name = name;
    this.quantity = quantity;
    this.price = price;
    this.subtotal = subtotal;
}